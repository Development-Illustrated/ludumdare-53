using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class secret : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("I luv dogs.");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
